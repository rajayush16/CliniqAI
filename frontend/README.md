# CliniqAI Frontend

React + Vite frontend for CliniqAI.

## Setup

```bash
cd frontend
npm install
cp .env.example .env
npm run dev
```

The app expects the backend at `VITE_API_BASE_URL`, defaulting to `http://localhost:8000`.

